/*
 * Generated by tools/bindings from the spec schemas (plan task 6.1). Do not edit: regenerate with
 * `cargo run -p pact_janus_bindings -- generate`.
 */


package io.pact.janus.bindings.shape.v1;

import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonPropertyDescription;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;


/**
 * Shape
 * <p>
 * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
 * 
 */
@JsonInclude(JsonInclude.Include.NON_NULL)
@JsonPropertyOrder({
    "alternatives",
    "content-type",
    "default",
    "discriminator",
    "encoded",
    "entries",
    "example",
    "format",
    "generator",
    "items",
    "keys",
    "max",
    "members",
    "min",
    "of",
    "options",
    "pattern",
    "shape",
    "substring",
    "values"
})
public class Shape {

    /**
     * Alternative name -> shape ('one-of'). Each alternative is an 'object' shape binding the discriminator to a distinct literal (spec.md §5.4).
     * 
     */
    @JsonProperty("alternatives")
    @JsonPropertyDescription("Alternative name -> shape ('one-of'). Each alternative is an 'object' shape binding the discriminator to a distinct literal (spec.md \u00a75.4).")
    private Map<String, io.pact.janus.bindings.shape.v1.Shape> alternatives;
    /**
     * Content type the value's octets must be detected as ('content-type').
     * 
     */
    @JsonProperty("content-type")
    @JsonPropertyDescription("Content type the value's octets must be detected as ('content-type').")
    private java.lang.String contentType;
    /**
     * Names the default variant point of this node: an alternative name ('one-of'). Absent, the default is the lexicographically first alternative — never JSON member order (spec.md §6.1).
     * 
     */
    @JsonProperty("default")
    @JsonPropertyDescription("Names the default variant point of this node: an alternative name ('one-of'). Absent, the default is the lexicographically first alternative \u2014 never JSON member order (spec.md \u00a76.1).")
    private java.lang.String _default;
    /**
     * Member name whose value selects the alternative ('one-of').
     * 
     */
    @JsonProperty("discriminator")
    @JsonPropertyDescription("Member name whose value selects the alternative ('one-of').")
    private java.lang.String discriminator;
    /**
     * Representation tag for 'example'. Absent means the example is its natural JSON value; 'base64' means it is the base64 of an octet sequence (Engine Protocol spec §2.4).
     * 
     */
    @JsonProperty("encoded")
    @JsonPropertyDescription("Representation tag for 'example'. Absent means the example is its natural JSON value; 'base64' means it is the base64 of an octet sequence (Engine Protocol spec \u00a72.4).")
    private java.lang.String encoded;
    /**
     * Positional element shapes ('array'), or the shapes that must each be satisfied by some element ('contains').
     * 
     */
    @JsonProperty("entries")
    @JsonPropertyDescription("Positional element shapes ('array'), or the shapes that must each be satisfied by some element ('contains').")
    private List<Shape> entries;
    /**
     * A value this shape admits, and the value produced for this node unless a variant point or generator selects another. For 'equality' it is also the operator's parameter: the shape admits exactly this value. Carries bytes when tagged (see 'encoded'); untagged it is the natural JSON value (Engine Protocol spec §2.5).
     * 
     */
    @JsonProperty("example")
    @JsonPropertyDescription("A value this shape admits, and the value produced for this node unless a variant point or generator selects another. For 'equality' it is also the operator's parameter: the shape admits exactly this value. Carries bytes when tagged (see 'encoded'); untagged it is the natural JSON value (Engine Protocol spec \u00a72.5).")
    private java.lang.Object example;
    /**
     * Date/time format pattern ('datetime', 'date', 'time'), in the pattern language fixed by spec.md §4.2. Optional: absent means the operator admits any string parsing as ISO-8601 for its kind.
     * 
     */
    @JsonProperty("format")
    @JsonPropertyDescription("Date/time format pattern ('datetime', 'date', 'time'), in the pattern language fixed by spec.md \u00a74.2. Optional: absent means the operator admits any string parsing as ISO-8601 for its kind.")
    private java.lang.String format;
    /**
     * Generator component reference and configuration (design 2.6), used where a value must be produced fresh rather than replayed from 'example'.
     * 
     */
    @JsonProperty("generator")
    @JsonPropertyDescription("Generator component reference and configuration (design 2.6), used where a value must be produced fresh rather than replayed from 'example'.")
    private Map<String, Object> generator;
    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("items")
    @JsonPropertyDescription("One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open \u2014 core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md \u00a74; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md \u00a73.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.")
    private Shape items;
    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("keys")
    @JsonPropertyDescription("One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open \u2014 core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md \u00a74; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md \u00a73.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.")
    private Shape keys;
    /**
     * Maximum cardinality ('each-like', 'each-entry'); absent means unbounded.
     * 
     */
    @JsonProperty("max")
    @JsonPropertyDescription("Maximum cardinality ('each-like', 'each-entry'); absent means unbounded.")
    private Long max;
    /**
     * Member name -> shape ('object'). Members not named here are admitted and ignored unless a member is declared 'forbidden' (spec.md §4.3).
     * 
     */
    @JsonProperty("members")
    @JsonPropertyDescription("Member name -> shape ('object'). Members not named here are admitted and ignored unless a member is declared 'forbidden' (spec.md \u00a74.3).")
    private Map<String, io.pact.janus.bindings.shape.v1.Shape> members;
    /**
     * Minimum cardinality ('each-like', 'each-entry'); default 1.
     * 
     */
    @JsonProperty("min")
    @JsonPropertyDescription("Minimum cardinality ('each-like', 'each-entry'); default 1.")
    private Long min;
    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("of")
    @JsonPropertyDescription("One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open \u2014 core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md \u00a74; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md \u00a73.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.")
    private Shape of;
    /**
     * The literal values admitted ('any-of'). Values, not shapes — a union of shapes is expressed with 'one-of' (spec.md §4.4).
     * 
     */
    @JsonProperty("options")
    @JsonPropertyDescription("The literal values admitted ('any-of'). Values, not shapes \u2014 a union of shapes is expressed with 'one-of' (spec.md \u00a74.4).")
    private List<java.lang.Object> options;
    /**
     * Regular expression ('regex'), in the dialect fixed by spec.md §4.2.
     * 
     */
    @JsonProperty("pattern")
    @JsonPropertyDescription("Regular expression ('regex'), in the dialect fixed by spec.md \u00a74.2.")
    private java.lang.String pattern;
    /**
     * The operator. Open vocabulary; an unknown operator is a named failure, never an ignored constraint (spec.md §3.7).
     * (Required)
     * 
     */
    @JsonProperty("shape")
    @JsonPropertyDescription("The operator. Open vocabulary; an unknown operator is a named failure, never an ignored constraint (spec.md \u00a73.7).")
    private java.lang.String shape;
    /**
     * Substring that must occur in the value ('include').
     * 
     */
    @JsonProperty("substring")
    @JsonPropertyDescription("Substring that must occur in the value ('include').")
    private java.lang.String substring;
    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("values")
    @JsonPropertyDescription("One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open \u2014 core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md \u00a74; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md \u00a73.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.")
    private Shape values;
    @JsonIgnore
    private Map<java.lang.String, java.lang.Object> additionalProperties = new LinkedHashMap<java.lang.String, java.lang.Object>();

    /**
     * Alternative name -> shape ('one-of'). Each alternative is an 'object' shape binding the discriminator to a distinct literal (spec.md §5.4).
     * 
     */
    @JsonProperty("alternatives")
    public Map<String, io.pact.janus.bindings.shape.v1.Shape> getAlternatives() {
        return alternatives;
    }

    /**
     * Alternative name -> shape ('one-of'). Each alternative is an 'object' shape binding the discriminator to a distinct literal (spec.md §5.4).
     * 
     */
    @JsonProperty("alternatives")
    public void setAlternatives(Map<String, io.pact.janus.bindings.shape.v1.Shape> alternatives) {
        this.alternatives = alternatives;
    }

    /**
     * Content type the value's octets must be detected as ('content-type').
     * 
     */
    @JsonProperty("content-type")
    public java.lang.String getContentType() {
        return contentType;
    }

    /**
     * Content type the value's octets must be detected as ('content-type').
     * 
     */
    @JsonProperty("content-type")
    public void setContentType(java.lang.String contentType) {
        this.contentType = contentType;
    }

    /**
     * Names the default variant point of this node: an alternative name ('one-of'). Absent, the default is the lexicographically first alternative — never JSON member order (spec.md §6.1).
     * 
     */
    @JsonProperty("default")
    public java.lang.String getDefault() {
        return _default;
    }

    /**
     * Names the default variant point of this node: an alternative name ('one-of'). Absent, the default is the lexicographically first alternative — never JSON member order (spec.md §6.1).
     * 
     */
    @JsonProperty("default")
    public void setDefault(java.lang.String _default) {
        this._default = _default;
    }

    /**
     * Member name whose value selects the alternative ('one-of').
     * 
     */
    @JsonProperty("discriminator")
    public java.lang.String getDiscriminator() {
        return discriminator;
    }

    /**
     * Member name whose value selects the alternative ('one-of').
     * 
     */
    @JsonProperty("discriminator")
    public void setDiscriminator(java.lang.String discriminator) {
        this.discriminator = discriminator;
    }

    /**
     * Representation tag for 'example'. Absent means the example is its natural JSON value; 'base64' means it is the base64 of an octet sequence (Engine Protocol spec §2.4).
     * 
     */
    @JsonProperty("encoded")
    public java.lang.String getEncoded() {
        return encoded;
    }

    /**
     * Representation tag for 'example'. Absent means the example is its natural JSON value; 'base64' means it is the base64 of an octet sequence (Engine Protocol spec §2.4).
     * 
     */
    @JsonProperty("encoded")
    public void setEncoded(java.lang.String encoded) {
        this.encoded = encoded;
    }

    /**
     * Positional element shapes ('array'), or the shapes that must each be satisfied by some element ('contains').
     * 
     */
    @JsonProperty("entries")
    public List<Shape> getEntries() {
        return entries;
    }

    /**
     * Positional element shapes ('array'), or the shapes that must each be satisfied by some element ('contains').
     * 
     */
    @JsonProperty("entries")
    public void setEntries(List<Shape> entries) {
        this.entries = entries;
    }

    /**
     * A value this shape admits, and the value produced for this node unless a variant point or generator selects another. For 'equality' it is also the operator's parameter: the shape admits exactly this value. Carries bytes when tagged (see 'encoded'); untagged it is the natural JSON value (Engine Protocol spec §2.5).
     * 
     */
    @JsonProperty("example")
    public java.lang.Object getExample() {
        return example;
    }

    /**
     * A value this shape admits, and the value produced for this node unless a variant point or generator selects another. For 'equality' it is also the operator's parameter: the shape admits exactly this value. Carries bytes when tagged (see 'encoded'); untagged it is the natural JSON value (Engine Protocol spec §2.5).
     * 
     */
    @JsonProperty("example")
    public void setExample(java.lang.Object example) {
        this.example = example;
    }

    /**
     * Date/time format pattern ('datetime', 'date', 'time'), in the pattern language fixed by spec.md §4.2. Optional: absent means the operator admits any string parsing as ISO-8601 for its kind.
     * 
     */
    @JsonProperty("format")
    public java.lang.String getFormat() {
        return format;
    }

    /**
     * Date/time format pattern ('datetime', 'date', 'time'), in the pattern language fixed by spec.md §4.2. Optional: absent means the operator admits any string parsing as ISO-8601 for its kind.
     * 
     */
    @JsonProperty("format")
    public void setFormat(java.lang.String format) {
        this.format = format;
    }

    /**
     * Generator component reference and configuration (design 2.6), used where a value must be produced fresh rather than replayed from 'example'.
     * 
     */
    @JsonProperty("generator")
    public Map<String, Object> getGenerator() {
        return generator;
    }

    /**
     * Generator component reference and configuration (design 2.6), used where a value must be produced fresh rather than replayed from 'example'.
     * 
     */
    @JsonProperty("generator")
    public void setGenerator(Map<String, Object> generator) {
        this.generator = generator;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("items")
    public Shape getItems() {
        return items;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("items")
    public void setItems(Shape items) {
        this.items = items;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("keys")
    public Shape getKeys() {
        return keys;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("keys")
    public void setKeys(Shape keys) {
        this.keys = keys;
    }

    /**
     * Maximum cardinality ('each-like', 'each-entry'); absent means unbounded.
     * 
     */
    @JsonProperty("max")
    public Long getMax() {
        return max;
    }

    /**
     * Maximum cardinality ('each-like', 'each-entry'); absent means unbounded.
     * 
     */
    @JsonProperty("max")
    public void setMax(Long max) {
        this.max = max;
    }

    /**
     * Member name -> shape ('object'). Members not named here are admitted and ignored unless a member is declared 'forbidden' (spec.md §4.3).
     * 
     */
    @JsonProperty("members")
    public Map<String, io.pact.janus.bindings.shape.v1.Shape> getMembers() {
        return members;
    }

    /**
     * Member name -> shape ('object'). Members not named here are admitted and ignored unless a member is declared 'forbidden' (spec.md §4.3).
     * 
     */
    @JsonProperty("members")
    public void setMembers(Map<String, io.pact.janus.bindings.shape.v1.Shape> members) {
        this.members = members;
    }

    /**
     * Minimum cardinality ('each-like', 'each-entry'); default 1.
     * 
     */
    @JsonProperty("min")
    public Long getMin() {
        return min;
    }

    /**
     * Minimum cardinality ('each-like', 'each-entry'); default 1.
     * 
     */
    @JsonProperty("min")
    public void setMin(Long min) {
        this.min = min;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("of")
    public Shape getOf() {
        return of;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("of")
    public void setOf(Shape of) {
        this.of = of;
    }

    /**
     * The literal values admitted ('any-of'). Values, not shapes — a union of shapes is expressed with 'one-of' (spec.md §4.4).
     * 
     */
    @JsonProperty("options")
    public List<java.lang.Object> getOptions() {
        return options;
    }

    /**
     * The literal values admitted ('any-of'). Values, not shapes — a union of shapes is expressed with 'one-of' (spec.md §4.4).
     * 
     */
    @JsonProperty("options")
    public void setOptions(List<java.lang.Object> options) {
        this.options = options;
    }

    /**
     * Regular expression ('regex'), in the dialect fixed by spec.md §4.2.
     * 
     */
    @JsonProperty("pattern")
    public java.lang.String getPattern() {
        return pattern;
    }

    /**
     * Regular expression ('regex'), in the dialect fixed by spec.md §4.2.
     * 
     */
    @JsonProperty("pattern")
    public void setPattern(java.lang.String pattern) {
        this.pattern = pattern;
    }

    /**
     * The operator. Open vocabulary; an unknown operator is a named failure, never an ignored constraint (spec.md §3.7).
     * (Required)
     * 
     */
    @JsonProperty("shape")
    public java.lang.String getShape() {
        return shape;
    }

    /**
     * The operator. Open vocabulary; an unknown operator is a named failure, never an ignored constraint (spec.md §3.7).
     * (Required)
     * 
     */
    @JsonProperty("shape")
    public void setShape(java.lang.String shape) {
        this.shape = shape;
    }

    /**
     * Substring that must occur in the value ('include').
     * 
     */
    @JsonProperty("substring")
    public java.lang.String getSubstring() {
        return substring;
    }

    /**
     * Substring that must occur in the value ('include').
     * 
     */
    @JsonProperty("substring")
    public void setSubstring(java.lang.String substring) {
        this.substring = substring;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("values")
    public Shape getValues() {
        return values;
    }

    /**
     * Shape
     * <p>
     * One shape node: an object discriminated by its 'shape' member. The operator vocabulary is open — core operators are listed in x-known-values, component-contributed operators are namespaced '<component>:<name>'. Per-operator arity is specified in spec.md §4; the branches below cover the core operators only, because an unknown operator's members cannot be checked here (spec.md §3.7). Recursive: 'of', 'items', 'keys', 'values', and the members of 'members', 'entries' and 'alternatives' are themselves shape nodes.
     * 
     */
    @JsonProperty("values")
    public void setValues(Shape values) {
        this.values = values;
    }

    @JsonAnyGetter
    public Map<java.lang.String, java.lang.Object> getAdditionalProperties() {
        return this.additionalProperties;
    }

    @JsonAnySetter
    public void setAdditionalProperty(java.lang.String name, java.lang.Object value) {
        this.additionalProperties.put(name, value);
    }

    @Override
    public java.lang.String toString() {
        StringBuilder sb = new StringBuilder();
        sb.append(Shape.class.getName()).append('@').append(Integer.toHexString(System.identityHashCode(this))).append('[');
        sb.append("alternatives");
        sb.append('=');
        sb.append(((this.alternatives == null)?"<null>":this.alternatives));
        sb.append(',');
        sb.append("contentType");
        sb.append('=');
        sb.append(((this.contentType == null)?"<null>":this.contentType));
        sb.append(',');
        sb.append("_default");
        sb.append('=');
        sb.append(((this._default == null)?"<null>":this._default));
        sb.append(',');
        sb.append("discriminator");
        sb.append('=');
        sb.append(((this.discriminator == null)?"<null>":this.discriminator));
        sb.append(',');
        sb.append("encoded");
        sb.append('=');
        sb.append(((this.encoded == null)?"<null>":this.encoded));
        sb.append(',');
        sb.append("entries");
        sb.append('=');
        sb.append(((this.entries == null)?"<null>":this.entries));
        sb.append(',');
        sb.append("example");
        sb.append('=');
        sb.append(((this.example == null)?"<null>":this.example));
        sb.append(',');
        sb.append("format");
        sb.append('=');
        sb.append(((this.format == null)?"<null>":this.format));
        sb.append(',');
        sb.append("generator");
        sb.append('=');
        sb.append(((this.generator == null)?"<null>":this.generator));
        sb.append(',');
        sb.append("items");
        sb.append('=');
        sb.append(((this.items == null)?"<null>":this.items));
        sb.append(',');
        sb.append("keys");
        sb.append('=');
        sb.append(((this.keys == null)?"<null>":this.keys));
        sb.append(',');
        sb.append("max");
        sb.append('=');
        sb.append(((this.max == null)?"<null>":this.max));
        sb.append(',');
        sb.append("members");
        sb.append('=');
        sb.append(((this.members == null)?"<null>":this.members));
        sb.append(',');
        sb.append("min");
        sb.append('=');
        sb.append(((this.min == null)?"<null>":this.min));
        sb.append(',');
        sb.append("of");
        sb.append('=');
        sb.append(((this.of == null)?"<null>":this.of));
        sb.append(',');
        sb.append("options");
        sb.append('=');
        sb.append(((this.options == null)?"<null>":this.options));
        sb.append(',');
        sb.append("pattern");
        sb.append('=');
        sb.append(((this.pattern == null)?"<null>":this.pattern));
        sb.append(',');
        sb.append("shape");
        sb.append('=');
        sb.append(((this.shape == null)?"<null>":this.shape));
        sb.append(',');
        sb.append("substring");
        sb.append('=');
        sb.append(((this.substring == null)?"<null>":this.substring));
        sb.append(',');
        sb.append("values");
        sb.append('=');
        sb.append(((this.values == null)?"<null>":this.values));
        sb.append(',');
        sb.append("additionalProperties");
        sb.append('=');
        sb.append(((this.additionalProperties == null)?"<null>":this.additionalProperties));
        sb.append(',');
        if (sb.charAt((sb.length()- 1)) == ',') {
            sb.setCharAt((sb.length()- 1), ']');
        } else {
            sb.append(']');
        }
        return sb.toString();
    }

    @Override
    public int hashCode() {
        int result = 1;
        result = ((result* 31)+((this._default == null)? 0 :this._default.hashCode()));
        result = ((result* 31)+((this.shape == null)? 0 :this.shape.hashCode()));
        result = ((result* 31)+((this.max == null)? 0 :this.max.hashCode()));
        result = ((result* 31)+((this.keys == null)? 0 :this.keys.hashCode()));
        result = ((result* 31)+((this.values == null)? 0 :this.values.hashCode()));
        result = ((result* 31)+((this.format == null)? 0 :this.format.hashCode()));
        result = ((result* 31)+((this.pattern == null)? 0 :this.pattern.hashCode()));
        result = ((result* 31)+((this.generator == null)? 0 :this.generator.hashCode()));
        result = ((result* 31)+((this.substring == null)? 0 :this.substring.hashCode()));
        result = ((result* 31)+((this.discriminator == null)? 0 :this.discriminator.hashCode()));
        result = ((result* 31)+((this.example == null)? 0 :this.example.hashCode()));
        result = ((result* 31)+((this.entries == null)? 0 :this.entries.hashCode()));
        result = ((result* 31)+((this.min == null)? 0 :this.min.hashCode()));
        result = ((result* 31)+((this.members == null)? 0 :this.members.hashCode()));
        result = ((result* 31)+((this.of == null)? 0 :this.of.hashCode()));
        result = ((result* 31)+((this.options == null)? 0 :this.options.hashCode()));
        result = ((result* 31)+((this.alternatives == null)? 0 :this.alternatives.hashCode()));
        result = ((result* 31)+((this.additionalProperties == null)? 0 :this.additionalProperties.hashCode()));
        result = ((result* 31)+((this.contentType == null)? 0 :this.contentType.hashCode()));
        result = ((result* 31)+((this.items == null)? 0 :this.items.hashCode()));
        result = ((result* 31)+((this.encoded == null)? 0 :this.encoded.hashCode()));
        return result;
    }

    @Override
    public boolean equals(java.lang.Object other) {
        if (other == this) {
            return true;
        }
        if ((other instanceof Shape) == false) {
            return false;
        }
        Shape rhs = ((Shape) other);
        return ((((((((((((((((((((((this._default == rhs._default)||((this._default!= null)&&this._default.equals(rhs._default)))&&((this.shape == rhs.shape)||((this.shape!= null)&&this.shape.equals(rhs.shape))))&&((this.max == rhs.max)||((this.max!= null)&&this.max.equals(rhs.max))))&&((this.keys == rhs.keys)||((this.keys!= null)&&this.keys.equals(rhs.keys))))&&((this.values == rhs.values)||((this.values!= null)&&this.values.equals(rhs.values))))&&((this.format == rhs.format)||((this.format!= null)&&this.format.equals(rhs.format))))&&((this.pattern == rhs.pattern)||((this.pattern!= null)&&this.pattern.equals(rhs.pattern))))&&((this.generator == rhs.generator)||((this.generator!= null)&&this.generator.equals(rhs.generator))))&&((this.substring == rhs.substring)||((this.substring!= null)&&this.substring.equals(rhs.substring))))&&((this.discriminator == rhs.discriminator)||((this.discriminator!= null)&&this.discriminator.equals(rhs.discriminator))))&&((this.example == rhs.example)||((this.example!= null)&&this.example.equals(rhs.example))))&&((this.entries == rhs.entries)||((this.entries!= null)&&this.entries.equals(rhs.entries))))&&((this.min == rhs.min)||((this.min!= null)&&this.min.equals(rhs.min))))&&((this.members == rhs.members)||((this.members!= null)&&this.members.equals(rhs.members))))&&((this.of == rhs.of)||((this.of!= null)&&this.of.equals(rhs.of))))&&((this.options == rhs.options)||((this.options!= null)&&this.options.equals(rhs.options))))&&((this.alternatives == rhs.alternatives)||((this.alternatives!= null)&&this.alternatives.equals(rhs.alternatives))))&&((this.additionalProperties == rhs.additionalProperties)||((this.additionalProperties!= null)&&this.additionalProperties.equals(rhs.additionalProperties))))&&((this.contentType == rhs.contentType)||((this.contentType!= null)&&this.contentType.equals(rhs.contentType))))&&((this.items == rhs.items)||((this.items!= null)&&this.items.equals(rhs.items))))&&((this.encoded == rhs.encoded)||((this.encoded!= null)&&this.encoded.equals(rhs.encoded))));
    }

}
