/*
 * Generated by tools/bindings from the spec schemas (plan task 6.1). Do not edit: regenerate with
 * `cargo run -p pact_janus_bindings -- generate`.
 */

/**
 * Known values of `Contract.$format` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ContractFormat = {
  JanusContract1: "janus-contract/1",
} as const;

/**
 * Known values of `Finding.code` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const FindingCode = {
  ExampleFrozenAsEquality: "example-frozen-as-equality",
  RuleUnmapped: "rule-unmapped",
  RuleCombinationOr: "rule-combination-or",
  RuleNarrowed: "rule-narrowed",
  GeneratorDropped: "generator-dropped",
  RequestBodyOpened: "request-body-opened",
  RequestQueryOpened: "request-query-opened",
  ContentTypeInferred: "content-type-inferred",
  BodyNotParsed: "body-not-parsed",
  StateParamsUntyped: "state-params-untyped",
  DuplicateDescription: "duplicate-description",
  InteractionDropped: "interaction-dropped",
} as const;

/**
 * Known values of `Finding.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const FindingKind = {
  Lossy: "lossy",
  Judgement: "judgement",
  Note: "note",
} as const;

/**
 * Known values of `SlotValue.encoded` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SlotValueEncoded = {
  Json: "json",
  Text: "text",
  Base64: "base64",
} as const;

/**
 * Known values of `Transport.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const TransportKind = {
  Http: "http",
  Https: "https",
  Message: "message",
} as const;

/**
 * Known values of `Transport.mode` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const TransportMode = {
  Passive: "passive",
  Emissive: "emissive",
} as const;
