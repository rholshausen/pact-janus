/*
 * Generated by tools/bindings from the spec schemas (plan task 6.1). Do not edit: regenerate with
 * `cargo run -p pact_janus_bindings -- generate`.
 */

/**
 * Known values of `CheckResult.format` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const CheckResultFormat = {
  JanusContract1: "janus-contract/1",
  Pact200: "pact/2.0.0",
  Pact300: "pact/3.0.0",
  Pact40: "pact/4.0",
} as const;

/**
 * Known values of `CompatibilityReport.$format` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const CompatibilityReportFormat = {
  JanusCompatibilityReport1: "janus-compatibility-report/1",
} as const;

/**
 * Known values of `ContractSource.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ContractSourceKind = {
  Inline: "inline",
} as const;

/**
 * Known values of `Decision` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const Decision = {
  Pass: "pass",
  Warn: "warn",
  Block: "block",
} as const;

/**
 * Known values of `EncodingName` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const EncodingName = {
  Json: "json",
} as const;

/**
 * Known values of `EngineError.category` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const EngineErrorCategory = {
  Protocol: "protocol",
  Session: "session",
  Document: "document",
  Component: "component",
  Internal: "internal",
} as const;

/**
 * Known values of `EngineError.code` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const EngineErrorCode = {
  MalformedFrame: "malformed-frame",
  HandshakeRequired: "handshake-required",
  ProtocolVersionUnsupported: "protocol-version-unsupported",
  OperationUnsupported: "operation-unsupported",
  CapabilityRequired: "capability-required",
  EngineShutDown: "engine-shut-down",
  SessionNotFound: "session-not-found",
  HandleNotFound: "handle-not-found",
  VariantNotFound: "variant-not-found",
  StreamNotFound: "stream-not-found",
  InteractionInvalid: "interaction-invalid",
  ContractInvalid: "contract-invalid",
  ContractVersionUnsupported: "contract-version-unsupported",
  VariantBudgetExceeded: "variant-budget-exceeded",
  ComponentUnavailable: "component-unavailable",
  ComponentFailed: "component-failed",
  Internal: "internal",
} as const;

/**
 * Known values of `Event.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const EventKind = {
  VerificationStarted: "verification/started",
  VerificationInteractionStarted: "verification/interaction-started",
  VerificationInteractionResult: "verification/interaction-result",
  VerificationHook: "verification/hook",
  VerificationExecutedPlan: "verification/executed-plan",
  VerificationFinished: "verification/finished",
} as const;

/**
 * Known values of `ExemptionResult.status` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ExemptionResultStatus = {
  Applied: "applied",
  Unused: "unused",
  Lapsed: "lapsed",
} as const;

/**
 * Known values of `ExplainSubject.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ExplainSubjectKind = {
  Spec: "spec",
  ContractInteraction: "contract-interaction",
} as const;

/**
 * Known values of `FindingEntry.disposition` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const FindingEntryDisposition = {
  Live: "live",
  Exempt: "exempt",
} as const;

/**
 * Known values of `Frame.type` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const FrameType = {
  Request: "request",
  Response: "response",
  Event: "event",
} as const;

/**
 * Known values of `InteractionResult.status` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const InteractionResultStatus = {
  Verified: "verified",
  Failed: "failed",
  NotExercised: "not-exercised",
} as const;

/**
 * Known values of `ObservedSlotValue.encoded` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ObservedSlotValueEncoded = {
  Base64: "base64",
} as const;

/**
 * Known values of `PolicyAction` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const PolicyAction = {
  Warn: "warn",
  Block: "block",
} as const;

/**
 * Known values of `Reason.action` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ReasonAction = {
  Note: "note",
  Warn: "warn",
  Block: "block",
} as const;

/**
 * Known values of `Reason.code` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const ReasonCode = {
  VerificationFailed: "verification-failed",
  VerificationIncomplete: "verification-incomplete",
  VerificationMissing: "verification-missing",
  VerificationFiltered: "verification-filtered",
  ProviderShapeMissing: "provider-shape-missing",
  InteractionsNotPublished: "interactions-not-published",
  SubsumptionFindings: "subsumption-findings",
  SubsumptionReviews: "subsumption-reviews",
  SubsumptionExempt: "subsumption-exempt",
  SubsumptionAdvisories: "subsumption-advisories",
  ExemptionLapsed: "exemption-lapsed",
  ExemptionNoExpiry: "exemption-no-expiry",
  ExemptionUnused: "exemption-unused",
  ExpiryNotEvaluated: "expiry-not-evaluated",
} as const;

/**
 * Known values of `RequestFrame.op` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const RequestFrameOp = {
  EngineHello: "engine/hello",
  EngineShutdown: "engine/shutdown",
  ConsumerSessionCreate: "consumer-session/create",
  ConsumerSessionAddInteraction: "consumer-session/add-interaction",
  ConsumerSessionVariants: "consumer-session/variants",
  ConsumerSessionStartTransport: "consumer-session/start-transport",
  ConsumerSessionServeVariant: "consumer-session/serve-variant",
  ConsumerSessionFinalise: "consumer-session/finalise",
  VerificationVerify: "verification/verify",
  VerificationExplain: "verification/explain",
  UpgradePact: "upgrade/pact",
  EventsPoll: "events/poll",
} as const;

/**
 * Known values of `StartTransport.transport` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const StartTransportTransport = {
  Http: "http",
} as const;

/**
 * Known values of `SubsumptionView.verdict` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SubsumptionViewVerdict = {
  Yes: "yes",
  No: "no",
  Unknown: "unknown",
  NotChecked: "not-checked",
} as const;

/**
 * Known values of `TransportBinding.transport` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const TransportBindingTransport = {
  Http: "http",
} as const;

/**
 * Known values of `UpgradeFinding.code` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const UpgradeFindingCode = {
  ExampleFrozenAsEquality: "example-frozen-as-equality",
  RuleUnmapped: "rule-unmapped",
  RuleCombinationOr: "rule-combination-or",
  RuleNarrowed: "rule-narrowed",
  GeneratorDropped: "generator-dropped",
  ContentTypeInferred: "content-type-inferred",
  BodyNotParsed: "body-not-parsed",
  StateParamsUntyped: "state-params-untyped",
  DuplicateDescription: "duplicate-description",
  InteractionDropped: "interaction-dropped",
} as const;

/**
 * Known values of `UpgradeFinding.kind` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const UpgradeFindingKind = {
  Lossy: "lossy",
  Judgement: "judgement",
  Note: "note",
} as const;

/**
 * Known values of `VariantResult.status` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const VariantResultStatus = {
  Verified: "verified",
  Failed: "failed",
  NotExercised: "not-exercised",
} as const;

/**
 * Known values of `VerificationView.status` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const VerificationViewStatus = {
  Verified: "verified",
  Failed: "failed",
  Incomplete: "incomplete",
  Unknown: "unknown",
} as const;
