/*
 * Generated by tools/bindings from the spec schemas (plan task 6.1). Do not edit: regenerate with
 * `cargo run -p pact_janus_bindings -- generate`.
 */

/**
 * Known values of `Note.code` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const NoteCode = {
  TargetDropped: "target-dropped",
  SizeSaturated: "size-saturated",
  BaseOnlySelected: "base-only-selected",
  VariantWaived: "variant-waived",
} as const;

/**
 * Known values of `SamplingPolicy.algorithm` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SamplingPolicyAlgorithm = {
  JanusIpogV1: "janus-ipog-v1",
} as const;

/**
 * Known values of `SamplingPolicy.strategy` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SamplingPolicyStrategy = {
  Auto: "auto",
  Exhaustive: "exhaustive",
  TWise: "t-wise",
  BaseOnly: "base-only",
} as const;

/**
 * Known values of `SelectionReport.algorithm` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SelectionReportAlgorithm = {
  JanusIpogV1: "janus-ipog-v1",
} as const;

/**
 * Known values of `SelectionReport.strategy` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const SelectionReportStrategy = {
  Auto: "auto",
  Exhaustive: "exhaustive",
  TWise: "t-wise",
  BaseOnly: "base-only",
} as const;

/**
 * Known values of `Variant.origin` — an open vocabulary: values not listed here may appear and must
 * be handled by policy, never rejected (engine-protocol spec §2.2 rule 3).
 */
export const VariantOrigin = {
  Base: "base",
  Boundary: "boundary",
  Pinned: "pinned",
  Covering: "covering",
} as const;
